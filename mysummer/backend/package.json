{
  "name": "mysummer-assist-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Local DeepSeek-backed travel planning assistant for the prototype",
  "scripts": {
    "start": "node src/server.js",
    "setup:platform": "node scripts/setup-platform.js",
    "start:local": "powershell -NoProfile -File ./start-local.ps1",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cloudbase/node-sdk": "3.18.3"
  }
}
